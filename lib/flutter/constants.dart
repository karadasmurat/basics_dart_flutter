import 'package:flutter/material.dart';

/// The horizontal padding
const EdgeInsets kHorizontalPadding = EdgeInsets.symmetric(horizontal: 16.0);

/// Bottom side of the rectangle with a circular radius
const BorderRadius kCircularBottomRadius = BorderRadius.vertical(
  bottom: Radius.circular(24),
);

/// Top side of the rectangle with a circular radius
const BorderRadius kCircularTopRadius = BorderRadius.vertical(
  top: Radius.circular(24),
);
